# Run this once on your homelab to create the secret:
#
#   kubectl create secret generic observability-secrets \
#     --namespace=betops \
#     --from-literal=GRAFANA_CLOUD_API_KEY=<your-token>
#
# Get your token from:
#   https://heftysofa1266.grafana.net/connections/add-new-connection/hmInstancePromId
